import std.io;

fn main() Int {
    text = read_or("input.txt", "6 7\\\\n");
    out = "Loaded bytes: ";
    n = len(text);
    write("output.txt", out + __to_json(n));
    print("wrote output.txt");
    return 0;
}
