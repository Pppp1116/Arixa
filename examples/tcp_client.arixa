import std.net;

fn main() Int{
    conn = tcp_connect("127.0.0.1:8080");
    if conn < 0 {
        print("connect failed");
        return 1;
    }
    else {}
    tcp_send(conn, "ping\n");
    reply = tcp_recv(conn, 256);
    print(reply);
    tcp_close(conn);
    return 0;
}
