# std.mem

Source: `stdlib/mem.astra`

Functions:

- `fill_bytes(buf, value) -> Int`
- `copy_bytes(dst, src) -> Int`

Both functions operate on `Bytes` (`Vec<u8>`) and return the count of elements written/copied.
