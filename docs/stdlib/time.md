# std.time

Source: `stdlib/time.astra`

Functions:

- `now_unix() -> Int`
- `now_ms() -> Int`
- `monotonic_ms() -> Int`
- `sleep_ms(ms) -> Int`
- `sleep_seconds(secs) -> Int`

Use `monotonic_ms` for elapsed-time measurement.
